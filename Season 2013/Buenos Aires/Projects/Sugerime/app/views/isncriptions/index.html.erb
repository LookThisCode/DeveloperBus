<h1>Listing isncriptions</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @isncriptions.each do |isncription| %>
  <tr>
    <td><%= link_to 'Show', isncription %></td>
    <td><%= link_to 'Edit', edit_isncription_path(isncription) %></td>
    <td><%= link_to 'Destroy', isncription, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Isncription', new_isncription_path %>
